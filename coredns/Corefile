.:53 {
    import /etc/coredns/generated/exact*.conf
    import /etc/coredns/generated/wildcards*.conf
    hosts /etc/coredns/generated/hosts {
        fallthrough
    }
    errors
    health
    forward . /etc/resolv.conf
}
